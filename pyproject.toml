[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "sazonal"
authors = [{name = "Framework", email = "frameworkcptec@gmail.com"}]
dynamic = ["version", "description"]
